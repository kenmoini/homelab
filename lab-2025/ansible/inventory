all:
  children:
    labcore:
      hosts:
        normandy:
          ansible_host: normandy.kemo.labs
          ansible_user: root
    moinimannor:
      hosts:
        galactica:
          ansible_host: galactica.moini.mannor
          ansible_user: root
    kemonetwork:
      hosts:
        citadel.corp.kemo.network:
          # Add this into /etc/hosts
          ansible_host: citadel.corp.kemo.network
          ansible_user: root